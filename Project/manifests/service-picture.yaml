apiVersion: v1
kind: Service
metadata:
  name: project-picture-svc
  namespace: project
spec:
  type: ClusterIP
  selector:
    app: project-picture
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080